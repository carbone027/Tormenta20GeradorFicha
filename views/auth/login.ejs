<%- include('../partials/header', { title: 'Login', activePage: 'login' }) %>

<section class="content-section active auth-section">
  <div class="auth-hero">
    <h2>🔐 Acesso ao Sistema</h2>
    <p>Entre com suas credenciais para acessar seus personagens e continuar suas aventuras em Arton</p>
  </div>
  
  <% if (typeof error !== 'undefined') { %>
    <div class="error-message">
      ⚠️ <%= error %>
    </div>
  <% } %>
  
  <div class="auth-container">
    <div class="auth-card">
      <div class="auth-header">
        <div class="auth-icon">⚔️</div>
        <h3>Bem-vindo de Volta, Aventureiro!</h3>
        <p>Suas fichas estão esperando por você</p>
      </div>
      
      <form action="/login" method="POST" class="auth-form" id="loginForm">
        <div class="form-group">
          <label for="username">👤 Nome de Usuário</label>
          <input type="text" id="username" name="username" required 
                 placeholder="Digite seu nome de usuário"
                 autocomplete="username">
          <div class="input-icon">👤</div>
        </div>
        
        <div class="form-group">
          <label for="password">🔑 Senha</label>
          <input type="password" id="password" name="password" required 
                 placeholder="Digite sua senha"
                 autocomplete="current-password">
          <div class="input-icon">🔑</div>
        </div>
        
        <div class="form-actions">
          <button type="submit" class="nav-btn login-btn">
            🎯 Entrar no Sistema
          </button>
        </div>
      </form>
      
      <div class="auth-divider">
        <span>ou</span>
      </div>
      
      <div class="auth-links">
        <p>Novo em Arton?</p>
        <a href="/register" class="register-link">
          ✨ Criar Nova Conta de Aventureiro
        </a>
      </div>
    </div>
    
    <!-- Informações do Sistema -->
    <div class="system-info">
      <div class="card info-card">
        <h4>🏰 Sistema Tormenta20</h4>
        <ul>
          <li>📋 Crie e gerencie personagens épicos</li>
          <li>🎲 Sistema completo de atributos</li>
          <li>🛡️ Base de dados de equipamentos</li>
          <li>⭐ Panteão completo de Arton</li>
          <li>💾 Salvamento seguro na nuvem</li>
        </ul>
      </div>
      
      <div class="card tips-card">
        <h4>💡 Dicas de Segurança</h4>
        <ul>
          <li>🔐 Use uma senha forte e única</li>
          <li>🚫 Nunca compartilhe suas credenciais</li>
          <li>💻 Sempre faça logout em computadores públicos</li>
          <li>📱 Acesse de qualquer dispositivo com segurança</li>
        </ul>
      </div>
    </div>
  </div>
</section>

<style>
/* Estilos específicos para autenticação */
.auth-section {
  max-width: 900px;
  margin: 2rem auto;
}

.auth-hero {
  text-align: center;
  margin-bottom: 3rem;
  padding: 2rem;
  background: linear-gradient(135deg, rgba(212, 175, 55, 0.1) 0%, rgba(204, 51, 51, 0.1) 100%);
  border-radius: 12px;
  border: 1px solid var(--accent-gold);
}

.auth-hero h2 {
  color: var(--accent-gold);
  font-size: 2.2rem;
  margin-bottom: 0.5rem;
}

.auth-hero p {
  color: var(--text-muted);
  font-size: 1.1rem;
  font-style: italic;
}

.auth-container {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 2rem;
  align-items: start;
}

.auth-card {
  background: var(--card-bg);
  border: 2px solid var(--border-color);
  border-radius: 12px;
  overflow: hidden;
  box-shadow: var(--shadow-lg);
  transition: all 0.3s ease;
}

.auth-card:hover {
  border-color: var(--accent-gold);
  box-shadow: var(--shadow-glow);
}

.auth-header {
  background: linear-gradient(135deg, var(--secondary-bg), var(--border-color));
  padding: 2rem;
  text-align: center;
  border-bottom: 2px solid var(--border-color);
}

.auth-icon {
  font-size: 3rem;
  margin-bottom: 1rem;
  display: inline-block;
  padding: 1rem;
  background: rgba(212, 175, 55, 0.2);
  border-radius: 50%;
  border: 3px solid var(--accent-gold);
}

.auth-header h3 {
  color: var(--accent-gold);
  margin-bottom: 0.5rem;
  font-size: 1.5rem;
}

.auth-header p {
  color: var(--text-muted);
  font-style: italic;
}

.auth-form {
  padding: 2rem;
}

.auth-form .form-group {
  margin-bottom: 1.5rem;
  position: relative;
}

.auth-form .form-group label {
  display: block;
  color: var(--accent-gold);
  font-weight: 600;
  margin-bottom: 0.5rem;
  font-size: 1rem;
}

.auth-form .form-group input {
  width: 100%;
  padding: 1rem 1rem 1rem 3rem;
  border: 2px solid var(--border-color);
  border-radius: 8px;
  background: var(--secondary-bg);
  color: var(--text-light);
  font-size: 1rem;
  transition: all 0.3s ease;
}

.auth-form .form-group input:focus {
  outline: none;
  border-color: var(--accent-gold);
  box-shadow: 0 0 0 3px rgba(212, 175, 55, 0.2);
  background: rgba(37, 40, 80, 0.9);
}

.input-icon {
  position: absolute;
  left: 1rem;
  top: 50%;
  transform: translateY(-50%);
  color: var(--text-muted);
  pointer-events: none;
  margin-top: 1rem;
}

.form-actions {
  margin-top: 2rem;
}

.login-btn {
  width: 100%;
  padding: 1rem;
  font-size: 1.1rem;
  font-weight: bold;
  background: var(--gradient-gold);
  transition: all 0.3s ease;
}

.login-btn:hover {
  transform: translateY(-2px);
  box-shadow: var(--shadow-md);
  filter: brightness(1.1);
}

.auth-divider {
  text-align: center;
  position: relative;
  margin: 1.5rem 0;
  color: var(--text-muted);
}

.auth-divider::before {
  content: '';
  position: absolute;
  top: 50%;
  left: 0;
  right: 0;
  height: 1px;
  background: var(--border-color);
  z-index: 1;
}

.auth-divider span {
  background: var(--card-bg);
  padding: 0 1rem;
  position: relative;
  z-index: 2;
}

.auth-links {
  text-align: center;
  padding: 1.5rem 2rem 2rem;
  border-top: 1px solid var(--border-color);
}

.auth-links p {
  color: var(--text-muted);
  margin-bottom: 1rem;
}

.register-link {
  display: inline-block;
  color: var(--accent-gold);
  text-decoration: none;
  padding: 0.8rem 1.5rem;
  border: 2px solid var(--accent-gold);
  border-radius: 8px;
  transition: all 0.3s ease;
  font-weight: 600;
}

.register-link:hover {
  background: var(--accent-gold);
  color: var(--primary-bg);
  transform: translateY(-2px);
}

/* System Info */
.system-info {
  display: flex;
  flex-direction: column;
  gap: 1.5rem;
}

.info-card,
.tips-card {
  background: linear-gradient(135deg, var(--card-bg) 0%, rgba(212, 175, 55, 0.05) 100%);
  border: 1px solid var(--border-color);
}

.info-card h4,
.tips-card h4 {
  color: var(--accent-gold);
  margin-bottom: 1rem;
  font-size: 1.2rem;
  text-align: center;
  border-bottom: 2px solid var(--accent-gold);
  padding-bottom: 0.5rem;
}

.info-card ul,
.tips-card ul {
  list-style: none;
  padding: 0;
}

.info-card li,
.tips-card li {
  background: rgba(37, 40, 80, 0.6);
  margin: 0.5rem 0;
  padding: 0.8rem;
  border-radius: 6px;
  border-left: 3px solid var(--accent-gold);
  color: var(--text-light);
}

/* Estados especiais */
.error-message {
  background: rgba(220, 53, 69, 0.2);
  border: 2px solid var(--error-red);
  color: #ff6b6b;
  padding: 1rem;
  border-radius: 8px;
  margin-bottom: 2rem;
  text-align: center;
  font-weight: 600;
}

/* Responsivo */
@media (max-width: 768px) {
  .auth-container {
    grid-template-columns: 1fr;
  }
  
  .auth-hero {
    padding: 1.5rem;
  }
  
  .auth-hero h2 {
    font-size: 1.8rem;
  }
  
  .auth-form,
  .auth-header {
    padding: 1.5rem;
  }
  
  .auth-icon {
    font-size: 2.5rem;
    padding: 0.8rem;
  }
}

/* Animações */
@keyframes slideInUp {
  from {
    opacity: 0;
    transform: translateY(30px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.auth-card {
  animation: slideInUp 0.6s ease-out;
}

.system-info .card {
  animation: slideInUp 0.6s ease-out 0.2s both;
}
</style>

<script>
document.addEventListener('DOMContentLoaded', function() {
  const form = document.getElementById('loginForm');
  const inputs = form.querySelectorAll('input');
  
  // Melhorar UX dos inputs
  inputs.forEach(input => {
    input.addEventListener('focus', function() {
      this.parentNode.classList.add('focused');
    });
    
    input.addEventListener('blur', function() {
      if (!this.value) {
        this.parentNode.classList.remove('focused');
      }
    });
    
    // Se já tem valor, manter focused
    if (input.value) {
      input.parentNode.classList.add('focused');
    }
  });
  
  // Submit do formulário
  form.addEventListener('submit', function(e) {
    e.preventDefault();
    
    // Feedback visual
    const btn = this.querySelector('.login-btn');
    const originalText = btn.textContent;
    
    btn.textContent = '🔄 Entrando...';
    btn.disabled = true;
    
    // Log dos dados para debug
    const formData = new FormData(form);
    const data = {};
    formData.forEach((value, key) => {
      data[key] = value;
    });
    console.log('Dados de login:', data);
    
    // Simular delay e submeter
    setTimeout(() => {
      form.submit();
    }, 500);
  });
  
  // Enter no password submete o form
  document.getElementById('password').addEventListener('keypress', function(e) {
    if (e.key === 'Enter') {
      form.submit();
    }
  });
});
</script>

<%- include('../partials/footer') %>